# 智通校园核心功能实现文档

## 功能概述

已完成智通校园项目的核心功能开发，包括：
1. ✅ 职位管理功能
2. ✅ 简历管理功能
3. ✅ 职位申请功能
4. ✅ 职位收藏功能
5. ✅ 用户管理功能

## 一、职位管理功能

### 后端实现
- **实体类**：`Job.java`
- **Mapper**：`JobMapper.java`
- **Service**：`JobService.java` 和 `JobServiceImpl.java`
- **Controller**：`JobController.java`

### API 接口
```
GET  /api/job/list                    - 获取所有职位列表
GET  /api/job/{id}                    - 获取职位详情
GET  /api/job/city/{city}             - 根据城市查询职位
GET  /api/job/industry/{industry}     - 根据行业查询职位
GET  /api/job/type/{jobType}          - 根据职位类型查询
```

### 前端对接
- ✅ `pages/campus/campus.js` - 校招页面已对接后端 API

---

## 二、简历管理功能

### 后端实现

#### 主要实体类
- `Resume.java` - 简历主表
- `ResumeEducation.java` - 教育经历
- `ResumeInternship.java` - 实习经历
- `ResumeProject.java` - 项目经历
- `ResumeAward.java` - 获奖经历

#### Service 层
- `ResumeService` - 简历服务
- `ResumeEducationService` - 教育经历服务
- `ResumeInternshipService` - 实习经历服务
- `ResumeProjectService` - 项目经历服务
- `ResumeAwardService` - 获奖经历服务

#### Controller 层
- `ResumeController` - 简历 CRUD
- `ResumeEducationController` - 教育经历 CRUD
- `ResumeInternshipController` - 实习经历 CRUD
- `ResumeProjectController` - 项目经历 CRUD
- `ResumeAwardController` - 获奖经历 CRUD

### API 接口

#### 简历接口
```
GET    /api/resume/list/{userId}      - 获取简历列表
GET    /api/resume/{id}                - 获取简历详情
POST   /api/resume                     - 创建简历
PUT    /api/resume                     - 更新简历
DELETE /api/resume/{id}                - 删除简历
```

#### 教育经历接口
```
GET    /api/resume/education/list/{resumeId}  - 获取列表
POST   /api/resume/education                  - 创建
PUT    /api/resume/education                  - 更新
DELETE /api/resume/education/{id}             - 删除
```

#### 实习经历接口
```
GET    /api/resume/internship/list/{resumeId} - 获取列表
POST   /api/resume/internship                 - 创建
PUT    /api/resume/internship                 - 更新
DELETE /api/resume/internship/{id}            - 删除
```

#### 项目经历接口
```
GET    /api/resume/project/list/{resumeId}    - 获取列表
POST   /api/resume/project                    - 创建
PUT    /api/resume/project                    - 更新
DELETE /api/resume/project/{id}               - 删除
```

#### 获奖经历接口
```
GET    /api/resume/award/list/{resumeId}      - 获取列表
POST   /api/resume/award                      - 创建
PUT    /api/resume/award                      - 更新
DELETE /api/resume/award/{id}                 - 删除
```

### 前端对接
- ✅ `pages/resume/resume.js` - 简历列表页面已对接

---

## 三、职位申请功能

### 后端实现
- **实体类**：`JobApplication.java`
- **Mapper**：`JobApplicationMapper.java`
- **Service**：`JobApplicationService.java` 和 `JobApplicationServiceImpl.java`
- **Controller**：`JobApplicationController.java`

### API 接口
```
POST   /api/application/apply                 - 申请职位
GET    /api/application/list/{userId}         - 获取申请列表
GET    /api/application/list/{userId}/{status} - 根据状态获取申请列表
DELETE /api/application/{id}?userId={userId}  - 撤销申请
GET    /api/application/check?userId={}&jobId={} - 检查是否已申请
GET    /api/application/{id}                  - 获取申请详情
```

### 业务逻辑
- 申请职位时检查是否已申请
- 撤销申请时验证用户权限
- 支持按状态筛选（pending/approved/rejected）

### 前端对接
- ✅ `pages/profile-applied/profile-applied.js` - 已投递页面已对接

---

## 四、职位收藏功能

### 后端实现
- **实体类**：`Favorite.java`
- **Mapper**：`FavoriteMapper.java`
- **Service**：`FavoriteService.java` 和 `FavoriteServiceImpl.java`
- **Controller**：`FavoriteController.java`

### API 接口
```
POST   /api/favorite/add                      - 添加收藏
DELETE /api/favorite/remove?userId={}&jobId={} - 取消收藏
GET    /api/favorite/list/{userId}            - 获取收藏列表
GET    /api/favorite/check?userId={}&jobId={} - 检查是否已收藏
POST   /api/favorite/toggle                   - 切换收藏状态
```

### 业务逻辑
- 添加收藏时检查是否已收藏
- 支持切换收藏状态（已收藏则取消，未收藏则添加）
- 使用唯一索引防止重复收藏

### 前端对接
- ✅ `pages/profile-favorite/profile-favorite.js` - 已收藏页面已对接

---

## 五、用户管理功能

### 后端实现
- **实体类**：`User.java`
- **Mapper**：`UserMapper.java`
- **Service**：`UserService.java` 和 `UserServiceImpl.java`
- **Controller**：`UserController.java`

### API 接口
```
GET  /api/user/{id}                    - 根据ID获取用户信息
GET  /api/user/openid/{openid}         - 根据openid获取用户信息
POST /api/user/login                   - 用户登录（创建或更新）
PUT  /api/user                         - 更新用户信息
GET  /api/user/points/{userId}         - 获取积分余额
POST /api/user/points/add              - 增加积分
POST /api/user/points/deduct           - 扣减积分
```

### 业务逻辑
- 支持微信登录（根据 openid 创建或更新用户）
- 积分管理（增加、扣减、查询余额）
- 扣减积分时检查余额是否足够

---

## 测试指南

### 1. 启动后端服务

使用 IntelliJ IDEA 或启动脚本启动后端服务。

### 2. 测试职位申请功能

```bash
# 申请职位
curl -X POST http://localhost:8080/api/application/apply \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"jobId":1,"resumeId":1}'

# 获取申请列表
curl http://localhost:8080/api/application/list/1

# 检查是否已申请
curl "http://localhost:8080/api/application/check?userId=1&jobId=1"

# 撤销申请
curl -X DELETE "http://localhost:8080/api/application/1?userId=1"
```

### 3. 测试职位收藏功能

```bash
# 添加收藏
curl -X POST http://localhost:8080/api/favorite/add \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"jobId":1}'

# 获取收藏列表
curl http://localhost:8080/api/favorite/list/1

# 检查是否已收藏
curl "http://localhost:8080/api/favorite/check?userId=1&jobId=1"

# 取消收藏
curl -X DELETE "http://localhost:8080/api/favorite/remove?userId=1&jobId=1"

# 切换收藏状态
curl -X POST http://localhost:8080/api/favorite/toggle \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"jobId":1}'
```

### 4. 测试用户管理功能

```bash
# 用户登录
curl -X POST http://localhost:8080/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"openid":"test_openid_123","nickname":"张三","avatar":"https://example.com/avatar.jpg"}'

# 获取用户信息
curl http://localhost:8080/api/user/1

# 获取积分余额
curl http://localhost:8080/api/user/points/1

# 增加积分
curl -X POST http://localhost:8080/api/user/points/add \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"points":100}'

# 扣减积分
curl -X POST http://localhost:8080/api/user/points/deduct \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"points":50}'
```

### 5. 测试前端小程序

1. 启动后端服务
2. 打开微信开发者工具
3. 测试以下功能：
   - **校招页面**：查看职位列表
   - **简历页面**：创建、编辑、删除简历
   - **已投递页面**：查看申请列表、撤销申请
   - **已收藏页面**：查看收藏列表、取消收藏

---

## 数据库表结构

### user 表
```sql
CREATE TABLE `user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `openid` VARCHAR(100) NOT NULL,
  `nickname` VARCHAR(50),
  `avatar` VARCHAR(255),
  `phone` VARCHAR(20),
  `email` VARCHAR(100),
  `points_balance` INT DEFAULT 0,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### job_application 表
```sql
CREATE TABLE `job_application` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `job_id` BIGINT NOT NULL,
  `resume_id` BIGINT,
  `status` VARCHAR(20) DEFAULT 'pending',
  `applied_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_job_id` (`job_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### favorite 表
```sql
CREATE TABLE `favorite` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `job_id` BIGINT NOT NULL,
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_job` (`user_id`, `job_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_job_id` (`job_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 前端 API 工具类

已在 `utils/api.js` 中添加了所有接口方法，包括：
- 职位相关接口（6个方法）
- 简历相关接口（20个方法）
- 用户相关接口（7个方法）
- 职位申请接口（6个方法）
- 收藏接口（5个方法）

**总计：44个 API 方法**

---

## 项目结构

```
src/main/java/com/example/ZhiTongXiaoYuan/
├── common/
│   └── Result.java                    # 统一响应结果
├── config/
│   ├── CorsConfig.java                # 跨域配置
│   └── MyBatisPlusConfig.java         # MyBatis-Plus配置
├── controller/
│   ├── TestController.java            # 测试控制器
│   ├── JobController.java             # 职位控制器
│   ├── ResumeController.java          # 简历控制器
│   ├── ResumeEducationController.java # 教育经历控制器
│   ├── ResumeInternshipController.java # 实习经历控制器
│   ├── ResumeProjectController.java   # 项目经历控制器
│   ├── ResumeAwardController.java     # 获奖经历控制器
│   ├── JobApplicationController.java  # 职位申请控制器
│   ├── FavoriteController.java        # 收藏控制器
│   └── UserController.java            # 用户控制器
├── entity/
│   ├── Job.java                       # 职位实体
│   ├── Resume.java                    # 简历实体
│   ├── ResumeEducation.java           # 教育经历实体
│   ├── ResumeInternship.java          # 实习经历实体
│   ├── ResumeProject.java             # 项目经历实体
│   ├── ResumeAward.java               # 获奖经历实体
│   ├── JobApplication.java            # 职位申请实体
│   ├── Favorite.java                  # 收藏实体
│   └── User.java                      # 用户实体
├── mapper/
│   ├── JobMapper.java
│   ├── ResumeMapper.java
│   ├── ResumeEducationMapper.java
│   ├── ResumeInternshipMapper.java
│   ├── ResumeProjectMapper.java
│   ├── ResumeAwardMapper.java
│   ├── JobApplicationMapper.java
│   ├── FavoriteMapper.java
│   └── UserMapper.java
└── service/
    ├── JobService.java
    ├── ResumeService.java
    ├── ResumeEducationService.java
    ├── ResumeInternshipService.java
    ├── ResumeProjectService.java
    ├── ResumeAwardService.java
    ├── JobApplicationService.java
    ├── FavoriteService.java
    ├── UserService.java
    └── impl/
        ├── JobServiceImpl.java
        ├── ResumeServiceImpl.java
        ├── ResumeEducationServiceImpl.java
        ├── ResumeInternshipServiceImpl.java
        ├── ResumeProjectServiceImpl.java
        ├── ResumeAwardServiceImpl.java
        ├── JobApplicationServiceImpl.java
        ├── FavoriteServiceImpl.java
        └── UserServiceImpl.java
```

---

## 功能完成度

### 已完成 ✅
1. ✅ 职位管理（查询、筛选）
2. ✅ 简历管理（CRUD + 子模块）
3. ✅ 职位申请（申请、撤销、查询）
4. ✅ 职位收藏（收藏、取消、查询）
5. ✅ 用户管理（登录、信息管理、积分管理）
6. ✅ 前端页面对接（校招、简历、已投递、已收藏）

### 待开发 🚧
1. 🚧 聊天会话功能（AI 助手）
2. 🚧 微信登录集成
3. 🚧 简历详情页面对接
4. 🚧 简历子模块页面对接
5. 🚧 职位详情页面
6. 🚧 简历导出功能

---

## 下一步开发建议

### 优先级 1：完善现有功能
1. **职位详情页面**
   - 显示职位完整信息
   - 添加申请按钮
   - 添加收藏按钮

2. **简历详情页面对接**
   - 修改 `resume-make.js`
   - 实现简历基本信息保存

3. **简历子模块页面对接**
   - 教育经历页面
   - 实习经历页面
   - 项目经历页面
   - 获奖经历页面

### 优先级 2：增强功能
1. **微信登录集成**
   - 实现微信授权登录
   - 获取用户 openid
   - 自动创建用户

2. **AI 助手功能**
   - 实现聊天会话管理
   - 集成 AI 对话接口
   - 实现职位推荐

3. **搜索和筛选优化**
   - 职位搜索功能
   - 高级筛选功能
   - 排序功能

### 优先级 3：扩展功能
1. **简历导出**
   - 导出为 PDF
   - 简历分享功能

2. **消息通知**
   - 申请状态变更通知
   - 新职位推荐通知

3. **数据统计**
   - 用户申请统计
   - 职位浏览统计

---

## 总结

智通校园项目核心功能已全部实现，包括：
- ✅ 9个实体类
- ✅ 9个 Mapper 接口
- ✅ 9个 Service 接口及实现
- ✅ 10个 Controller
- ✅ 44个 API 接口
- ✅ 前端 4个页面对接

项目已具备完整的求职招聘功能，可以进行端到端测试！
